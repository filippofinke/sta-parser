from setuptools import setup, find_packages

setup(
    name='sta_parser',
    version='1.0.0',
    author='Filippo Finke',
    author_email='filippofinke@protonmail.ch',
    description='A parser for the OGC SensorThings API standard',
    packages=find_packages(),
    install_requires=[
    ],
)
